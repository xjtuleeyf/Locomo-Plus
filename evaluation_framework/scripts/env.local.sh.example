# Local API config (do not commit real credentials).
# Copy to env.local.sh and set your values. Compatible with OpenAI client (base_url + api_key).
# export OPENAI_BASE_URL=""
# export OPENAI_API_KEY=""
